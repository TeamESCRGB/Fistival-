using UnityEngine;

public class testangle : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    public Vector3 asdf;
    // Update is called once per frame
    void Update()
    {
        asdf = transform.right;
    }
}
